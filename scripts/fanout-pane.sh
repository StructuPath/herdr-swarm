#!/usr/bin/env bash
# Scaffold stub — the real fan-out pane lands in U4 of
# docs/plans/2026-07-22-001. The sleep keeps the message on screen: a pane
# whose process exits immediately closes before the user can read it, which
# reads as a crash.
echo "herdr-swarm: the Swarm Fan-out pane is not implemented yet (repo scaffold only)."
sleep 600
