#!/usr/bin/env bash
# Scaffold stub — the real fan-out action (thin opener for the fan-out pane)
# lands in U4 of docs/plans/2026-07-22-001. Exits 0 with a message: a naked
# failure here would make a linked-but-unbuilt plugin read as broken.
echo "herdr-swarm: 'fanout' is not implemented yet (repo scaffold only)."
exit 0
