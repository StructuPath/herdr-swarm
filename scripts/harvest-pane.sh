#!/usr/bin/env bash
# Scaffold stub — the real harvest pane lands in U6 of
# docs/plans/2026-07-22-001. The sleep keeps the message on screen: a pane
# whose process exits immediately closes before the user can read it, which
# reads as a crash.
echo "herdr-swarm: the Swarm Harvest pane is not implemented yet (repo scaffold only)."
sleep 600
